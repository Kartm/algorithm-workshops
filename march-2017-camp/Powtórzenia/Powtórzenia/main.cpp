#include <iostream>
#include <string>
#include <stdio.h>
#include <algorithm>
#include <string>

using namespace std;
//znajdŸ 3 powtórzenia

int main()
{
    int testAmount; scanf("%d", &testAmount);
    int resultArray[testAmount];

    for(int i = 0; i < testAmount; i++){
        int numberAmount; scanf("%d", &numberAmount);
        int numberArray[numberAmount];
        for(int i = 0; i < numberAmount; i++){\
            scanf("%d", &numberArray[i]);
        }
        bool hereItIs = false;
        while(hereItIs == false){

        }
    }
    return 0;
}
