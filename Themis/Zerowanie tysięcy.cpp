#include <iostream>
#include <vector>
#include <string>

using namespace std;
int main(){
    int number; cin >> number;
    if(number > 999){
        int zeros = number.length
        int up = number - (numer % 10000);
    }
    return 0;
}
